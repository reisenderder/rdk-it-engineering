# Changelog

Все заметные изменения внешнего вида сайта RDK IT Engineering фиксируются здесь.
Формат основан на «Keep a Changelog»; версии не проставляются — проект
одностраничный, ориентир — дата.

## [Unreleased] — раунд 2: тексты и визуал

По итогам аудита копирайта и сравнения с Refero (опорные референсы: **Groq** —
почти близнец: тёплые off-white + один акцент-«сигнальная лампа» + моно для
техники + чередование светлых и тёмных секций; **(dot)connect** — акцент как
фоновая заливка, не CTA; **SST** — «конфиг на бумаге», mono-заголовок, радиусы
4–8px; **Spacelab / 19-86** — архитектурный минимализм, ноль теней).

Порядок: Е1 → Е2 → Ж1–Ж6 → З1 … З16 → З17 → Е3.
**Статус:** Е1–Е2, Ж1–Ж6, З1–З17 — сделаны. **Блок З (механизм hero) закрыт:**
заказчик проверил на телефоне и компьютере, принял; текущая версия
`?v=20260903-z17`. Ж5 — оставлено без правок по решению заказчика. Остаётся
**Е3** (контакты + форма) — заказчик готовит данные; добавим вместе с
переработкой блока `.contact`.

Бэкап перед З9: `_backup/2026-09-03-z8-before-mobile/` (index/styles/motion/script/CHANGELOG на версии `?v=z8`).

### БЛОК И — Правки прайса и Instagram у «Кода Наследия» *(2026-09-03, `?v=20260903-z18`)*

Файлы: `index.html`, `styles.css`.

- [x] **И1. Обновление ориентиров цены** — Landing Page: к заголовку добавлено
      пояснение `(одностраничный сайт)` (mono, `--muted`, класс `.price-sub`),
      цена `от 70 000 ₽` → `от 40 000 ₽`; Custom Product `от 500 000 ₽` →
      `от 400 000 ₽`; Bot Start `от 70 000 ₽` → `от 40 000 ₽`; Connected Bot
      `от 250 000 ₽` → `от 180 000 ₽`. Остальные строки прайса без изменений.
- [x] **И2. Ссылка на Instagram в блоке «Код Наследия»** — в `.heritage-copy`
      под списком добавлена ссылка «Следить за проектом в Instagram» на
      `instagram.com/rdk_it_engineering` с иконкой. В спрайт добавлен
      `#i-instagram` (обводка, единый стиль с `#i-arrow`); класс
      `.heritage-social` — акцентная иконка, hover в бирюзу, вписан в общий
      минимализм секции.

### БЛОК Е — Тексты и смыслы

Файлы: `index.html`, `script.js`.

- [x] **Е1. Декорация под видом информации** *(2026-09-02)* — удалён
      `heritage-note` («Ассоциация с сериалом живёт в линиях…» — мета-
      комментарий о самом дизайне, обращённый в никуда) + его CSS-правило;
      из ядра механизма убран `DIGITAL CALIBRE` (осталось `RDK`) + осиротевшее
      правило `.disc-core small`; `PRINCIPLE / 001` → `ПРИНЦИП / 01`;
      состояние механизма `SYSTEM STABLE` / `CORE TRACKING` →
      `СТАБИЛЬНО` / `ОТКЛИК` (`index.html` + `script.js`). Чистые приборные
      метки `CORE / 01` и `Архитектура · Код · Контроль` оставлены.
- [x] **Е2. Точечный копирайт** *(2026-09-02)*:
      status-line «Принимаем проекты на разработку» → «Открыты для новых проектов»;
      `og:title` синхронизирован с h1 («…в работающую систему»);
      шапки панелей `01 / Products` (`/ Automation`, `/ Engineering`) → `01`/`02`/`03`
      (англ. дубль русского h3 убран);
      price-disclaimer «Непредвиденные задачи не появляются в счёте без
      согласования» → «Работы вне спецификации — только после согласования»;
      footer «Reliability · Digital · Komplex» → «Разработка · Данные · Контроль»
      (бэкроним R-D-K сохранён, но все слова — реальные русские);
      heritage-lead — убрана натянутая параллель, теперь:
      «Собственный сериал RDK Lab. Делаем его сами — от сценария и культурного
      исследования до 3D-графики, AI-производства и выпуска».
- [ ] **Е3. Контакты и форма** *(ОТЛОЖЕН)* — сейчас единственный контакт
      Instagram Direct; для B2B с чеками 0.5–0.9 млн ₽ нужен e-mail +
      Telegram + короткая форма. Ждём данные от заказчика.

### БЛОК Ж — Визуал (по мотивам Refero)

Файлы: `index.html`, `styles.css`.

- [x] **Ж1. Настоящий моноширинный шрифт** *(2026-09-02)* — подключён
      **IBM Plex Mono** (400;500, latin+cyrillic) через тот же Google Fonts
      `<link>`; `--mono` → `"IBM Plex Mono", "SFMono-Regular", Consolas, …`.
      Теперь приборные подписи (scope-теги, `CORE / 01`, `01/02/03`,
      вертикальная подпись механизма, hero-foot, price-label) рендерятся
      одинаково на всех машинах и звучат «инженернее». Вес 500 не грузится,
      пока не используется (Google Fonts подтягивает подмножество по факту).
      Трекинг mono-меток пока не трогал — проверю при необходимости.
- [x] **Ж2. Доктрина акцента** *(2026-09-02, выбран гибрид)* —
      **свели тонкое присутствие к минимуму:** бирюза убрана из `.eyebrow`
      (→ `--muted`), `.price-label` (→ `--muted`), номеров `.hero-facts dt` /
      `.method-list li>span` / `.heritage-list span` (→ `--faint`),
      `.brand-mark` (→ `--ink`), стрелки `.text-link` (→ наследует `--ink`).
      Акцент оставлен строго на: CTA (`.button-primary`, `.nav-action`),
      точке статуса «Открыты для новых проектов» (единственная буквальная
      «лампа»), рекомендованном пакете и активной вкладке (В2), hover-
      состояниях, функциональных элементах (scroll-progress, `::selection`,
      фокус), внутренней подсветке механизма.
      **+ одна акцентная полоса:** блок `.principle` переведён с графита
      `#22251f` на глубокую бирюзу `--accent-deep` #014b52 (токен
      `--surface-ink` переименован); текст #f2f1ec, метка «ПРИНЦИП / 01»
      — светлый мятный #7fd4d7. Секция теперь и тёмный якорь, и смелый
      цветовой момент (приём фоновой заливки как у (dot)connect).
- [x] **Ж3. Контейнировать механизм** *(2026-09-02)* — на десктопе
      `.calibre-wrap` стал рамкой: `padding` clamp(20–44px), `border` 1px
      `--line`, `border-radius` `--radius-lg`, лёгкий градиентный фон,
      `overflow:hidden` (обрезает вылезающие лазеры/орбиты). Диск уменьшен
      `min(35vw,430) → min(26vw,300)`, вторая колонка hero `.82fr → .85fr`.
      Лазеры втянуты `width:116%/left:-8% → 100%/0`. Убран `margin-top:-24px`
      (рамка центрируется в ячейке). Readout и вертикальная подпись переставлены
      в углы рамки (`--space-4/5`).
      **Ниже 1000px механизм скрыт полностью** (`.calibre-wrap{display:none}`)
      вместо прежнего ghost-диска за текстом (`opacity:.37/.17`, `right:-230/-285`);
      `.hero{min-height:auto}` + `.hero-foot{display:none}` в том же брейкпоинте —
      hero стал плотным и без пустот. JS механизма корректно простаивает на
      скрытом элементе (консоль чистая).
- [x] **Ж4. Убрать тени** *(2026-09-02)* — `.heritage-media`: `box-shadow`
      (единственная настоящая elevation-тень на странице) → `border` 1px
      `--line` (фото теперь «вставлено», а не «висит» — как рамка механизма);
      токен `--shadow` удалён; `.contact-orbit` — убраны декоративные
      concentric-ripple `box-shadow` (осталось только кольцо-бордюр).
      Оставлены: внутренние тени механизма (часть 3D-иллюзии, механизм
      защищён), inset-полоски акцента у вкладки/пакета (индикатор, не
      elevation), halo вокруг точки статуса (сигнал), `backdrop-filter`
      прилипшей шапки (функция, не тень). Глубина теперь — тон секций,
      hairline-бордюры и одна глубокая полоса.
- [~] **Ж5. Форма элементов** *(2026-09-02, решение: оставить)* — заказчик
      выбрал сохранить текущие радиусы 8/16px (середина между «чертёжным»
      SST/19-86 и «мягким» Groq/(dot)connect). Правок нет.
- [x] **Ж6. Заголовок-монумент** *(2026-09-02)* — `--fs-h1`
      `clamp(44,6vw,78) → clamp(44,6.4vw,92)`; `.hero h1` line-height
      `1.05 → 1`, трекинг `-.02em → -.025em`, `max-width 810 → 900`.
      На 1280 h1 ≈ 82px (было 78), на 1440+ ≈ 92px — заполняет колонку в
      3 строки, читается как монумент; мобильный h1 (свой clamp) не затронут.

### БЛОК З — Механизм: возврат формата + орбиты + вращение сферы

Файлы: `index.html`, `styles.css`, `motion.css`.

Причина: после недокументированного раунда `motion.css` механизм потерял
выразительность — орбиты замедлились (54с/72с) и «закувыркались» по трём осям,
передний проход орбиты обрезан `clip-path` до полоски, всё приглушено; плюс в
блоке Ж3 диск ужат (`min(35vw,430) → min(26vw,300)`) и посажен в рамку-контейнер.
Заказчик хочет вернуть «прежний формат» (крупный диск без рамки, лазеры во всю
ширину, ровные заметные орбиты — как на присланном референсе, но подписи
`DIGITAL CALIBRE` / `SYSTEM STABLE` НЕ возвращаем, они убраны в Е1), добавить
выразительности орбитам и третью орбиту, и заставить сферу вращаться «как Земля
вокруг оси» при неподвижном центре-ядре с «RDK».

Решения заказчика: центр (ядро с «RDK» и мини-глобусом) — неподвижен, надпись
всегда читаема; вращение — вариант 2C (текстура сферы под стеклянным куполом +
каркас меридианов); диск возвращаем к `min(35vw,430px)`; рамку-контейнер
убираем. Идле-параллакс/дыхание/блик от `motion.css` НЕ трогаем (не мешают
целям, убирать — значит делать механизм безжизненнее).

- [x] **З1. Снять рамку-контейнер, вернуть размер** *(2026-09-03)* —
      `.calibre-wrap`: убраны `border`, градиентный фон, `overflow:hidden`,
      `padding` (осталось `position:relative`, `display:grid`,
      `place-items:center`, `perspective:1200px`); `min-height`
      `min(46vw,420px) → min(52vw,480px)` под более крупный диск. `.calibre`
      `min(26vw,300px) → min(35vw,430px)`. Лазеры `width:100%/left:0 →
      width:116%/left:-8%` (откат втягивания из Ж3). `?v=` → `20260903-z1`.
      Проверено (computed styles + геометрия + рендер на 700px): рамки нет,
      диск 430px, лазеры 116%, горизонтального скролла нет (обрезаются
      `.hero{overflow:hidden}`), диск не перекрывает h1/лид (зазор ~72px),
      вертикальная подпись слева не задевает диск, readout `CORE / 01`
      подходит к нижней кромке диска близко — как на референсе. Консоль
      чистая, идле-параллакс и CSS-анимации работают. Брейкпоинт `<1000px`
      (`.calibre-wrap{display:none}`) не тронут.
- [x] **З2. Откат надстроек `motion.css` для орбит** *(2026-09-03)* —
      удалены переопределения `@keyframes orbit-a/-b` (тумбл по трём осям) —
      вращение снова ровное из `styles.css` (наклон + прокрутка по одной
      оси); удалены `.orbit-a/-b{animation-duration:54s/72s}` → штатные
      18с/24с. `.orbit-front`: `clip-path` `inset(49%…)` (полоска 51%) →
      `inset(44% -25%…)` (мягкая ближняя дуга 56%), снято приглушение
      `border-color .12 → .17`. Полностью удалён CSS мини-глобуса
      (`.core-globe`, `.core-globe::before/ i`, `.globe-meridian` + longitude,
      `.globe-latitude*`, `@keyframes globe-meridian-turn`) — за «RDK» чисто;
      разметка `.core-globe` оставлена в `index.html` как контейнер под З5
      (инертна: `position:static`, 0×0). В reduced-motion убрана строка
      `.globe-meridian`. Оставлены: `calibre-breath`, `glass-glint`,
      `signal-breath`, `laser-breath`, стекинг `orbit-back/front`, 3D-контекст
      `.disc-core`, z-index «RDK» (z3) / сигнала (z4), идле-параллакс.
      `?v=` → `20260903-z2`. Проверено (computed styles на 1280): активен
      только `@keyframes orbit-a` из `styles.css`, `.calibre` держит
      matrix3d-параллакс, `.core-globe` инертна, горизонтального скролла нет,
      консоль чистая.
- [x] **З3. Выразительность орбит** *(2026-09-03)* — `styles.css`: обводка
      `.orbit` `rgba(39,39,32,.17) → .32`; ведущая `.orbit-a` — бирюзовый тон
      `rgba(1,106,113,.3)`. Под-кольца разведены: `::before` `inset 8% → 9%`
      (`.1 → .16`), `::after` `inset 8% → 21%` (`.11`) — тройная линия у
      каждой орбиты. Частицы `8px → 9px`, свечение усилено
      (`0 0 16px .5 + 0 0 5px .7`); добавлен хвост-комета `.orbit i::after`
      (тонкий градиент 3×26px по локальному направлению движения — вверх у
      `.orbit-a`, вниз у идущей реверсом `.orbit-b`). `.orbit-b i` — вернулось
      лёгкое серое свечение (было `box-shadow:none`), цвет `#a3a69f → #9a9d95`.
      `motion.css`: `.orbit-front::before/::after` `display:none → opacity:.7`
      (под-кольца видны и на переднем проходе, обрезаются общим `clip-path`),
      `.orbit-front i` `opacity .8 → .85`. `?v=` → `20260903-z3`. Проверено
      (computed styles на 1280 + анимированный превью): обводки применились,
      комета рендерится, под-кольца переднего прохода `display:block`,
      горизонтального скролла нет, консоль чистая.
- [x] **З4. Третья орбита** *(2026-09-03)* — заказчик выбрал «почти
      вертикальная, перпендикулярно двум текущим» (читается как гироскоп/атом).
      `index.html`: добавлены `.orbit-c.orbit-back` и `.orbit-c.orbit-front`
      (с частицей) рядом с a/b. `styles.css`:
      `.orbit-c{inset:1%;transform:rotateY(-16deg) rotateX(46deg) rotateZ(52deg);
      animation:orbit-c 30s linear infinite;border-color:rgba(39,39,32,.24)}`,
      `@keyframes orbit-c` (rotateZ 52→412); под-кольца отключены
      (`.orbit-c::before/::after{display:none}` — одиночная линия, чтобы не
      перегружать); частица серая как у `.orbit-b`, хвост-комета вверх
      (идёт вперёд). Скорость 30с — не кратна 18с/24с. `motion.css`:
      `.orbit-c` в reduced-motion — 96с. `?v=` → `20260903-z4`. Проверено
      (1280): 3 back + 3 front орбиты, `@keyframes orbit-c` активен, частица
      `#9a9d95`, горизонтального скролла нет, консоль чистая.
- [x] **З5. Вращающаяся сфера 2C** *(2026-09-03)* — `index.html`: новый слой
      `.disc-sphere` между `.disc-scale` и `.disc-glass` (порядок в DOM =
      порядок отрисовки: под стеклом, над щёткой). `styles.css`:
      `.disc-sphere` — вертикальные меридианы `repeating-linear-gradient(90deg,
      rgba(39,39,32,.3) 0 1.5px, transparent 1.5px 40px)`, маска-эллипс
      `radial-gradient(ellipse 64% 90% …)` (гуще к центру, тают к лимбу),
      `animation: disc-spin 13s` (сдвиг `background-position` на шаг 40px =
      бесшовный цикл, ощущение вращения вокруг вертикальной оси).
      `@keyframes disc-spin` рядом с `disc-turn`. Центр-«глобус»: разметка
      `.core-globe` снова в деле — статичная 2D-сетка (3 меридиана
      `scaleX .32/.64/.9` + экватор `.globe-latitude`, 4-й меридиан и 2-я
      широта `display:none`), маска-круг, бирюза `.13`, **не** анимируется.
      `.disc-core` — фон `.76 → .84` (контраст «RDK» над сеткой), «RDK»
      `z-index:3`, `.core-signal` `z-index:4` (дублируют правила `motion.css`
      для устойчивости базового файла). `motion.css`: `.disc-sphere` в
      reduced-motion — 44с; из `.disc-core` убраны ненужные теперь
      `perspective`/`preserve-3d`, комментарий обновлён. `?v=` →
      `20260903-z5`. Проверено (computed styles 1280 + анимированный превью):
      слой-сфера в стопке между scale и glass, `disc-spin` активен, `.core-globe`
      z1 с `scaleX(.32)` на первом меридиане, «RDK» z3, горизонтального скролла
      нет, консоль чистая. Скорость/плотность — кандидаты на подстройку в З6.
- [x] **З6. Reduced-motion + QA** *(2026-09-03)* — правок кода не
      потребовалось. Reduced-motion: все анимированные слои механизма перекрыты
      медленными длительностями, включая новые `.disc-sphere` (44с) и
      `.orbit-c` (96с); `.calibre` держит идле-параллакс (`transform` не
      `none`); `.core-globe` статична (правильно — это неизменный центр).
      Прогон по ширинам: 1920 / 1440 / 1001 — механизм влезает в правую
      колонку hero, не перекрывает h1/лид (зазор 58–305px), лазеры 116% и
      орбиты обрезаются `.hero{overflow:hidden}`, горизонтального скролла нет;
      1000 и ниже — `.calibre-wrap{display:none}`, hero плотный, скролла нет;
      375 — чисто. Консоль чистая на всех ширинах. `?v=` остаётся
      `20260903-z5` (в З6 код не менялся).
- [x] **З7. Орбиты на 360° + сфера крутится заметнее** *(2026-09-03, по
      фидбеку)* — заказчик: «орбиты должны двигаться вокруг сферы на 360°, и
      сама сфера тоже должна крутиться». До этого кольца стояли на месте
      (двигалась только точка), меридианы сферы ползли еле заметно.
      **Орбиты:** отказ от схемы «кольцо в двух копиях back/front + `clip-path`»
      — теперь одно кольцо на орбиту (`index.html`: убраны `.orbit-*-back`,
      с `.orbit-*-front` снят суффикс), `z-index:2` над диском. Каждое кольцо
      делает полный оборот вокруг своей оси — ровно, одноосно, без «кувыркания»
      блока Ж (`@keyframes orbit-a/-b/-c` → `rev-a` `rotateY 0→360`,
      `rev-b` `rotateX 0→360` при `rotateZ(-26)`, `rev-c` `rotateX 0→360` при
      `rotateZ(56)`; 15/21/27с, b — reverse). Частица теперь бежит по самому
      кольцу через `offset-path:circle(closest-side)` + `offset-rotate:auto`
      (5/6.5/8с), хвост-комета `i::after` развёрнут по направлению движения
      (`right:100%`, `linear-gradient(to left …)`). Обводки чуть спокойнее
      (`.32→.3`, под-кольца `.16→.15` / `.11→.1`) — компенсация возросшего
      движения. **Сфера:** `.disc-sphere` — `opacity .4→.52`, меридианы
      `.3→.36` и плотнее (`40px→34px`), быстрее (`13с→8с`); добавлен слой
      `.disc-sphere::before` — бегущая полутень-терминатор
      (`linear-gradient` + `background-size:230%`, `disc-shade` 12с).
      `motion.css`: блок `.orbit-back/.orbit-front` заменён на `.orbit{z-index:2}`
      + `will-change`; reduced-motion — `rev-*` 132/168/150с, `run` 34/40/46с,
      `disc-spin` 30с, `disc-shade` 40с (всё «живо, но очень спокойно»).
      `?v=` → `20260903-z7`. Проверено (computed styles + анимированный превью,
      ширины 1001/1280): 3 одиночных кольца, `rev-*`/`run`/`disc-shade`
      активны, `offset-path` применяется, кольца в разных положениях в разные
      моменты (оборот идёт), горизонтального скролла нет (`.hero{overflow:hidden}`
      срезает 4px на 1001), консоль чистая. `offset-path` — Chrome 116+,
      Firefox, Safari 16+.
- [x] **З8. Механизм остаётся на мобильном** *(2026-09-03, по фидбеку)* —
      разворот решения Ж3 («ниже 1000px механизм скрыт полностью»): заказчик
      хочет видеть сферу и орбиты на телефоне. В `@media(max-width:1000px)`:
      `.calibre-wrap` `display:none` → видим (падает на базовый `grid`,
      встаёт в одну колонку под `.hero-copy`, перед скрытым `.hero-foot`),
      `margin-top:var(--space-7)`, `min-height:min(76vw,340px)`; `.calibre`
      `width:min(64vw,300px)` (на десктопе остаётся `min(35vw,430px)`) — на
      375px диск ≈ 240px, на 720–1000px — 300px; угловые подписи
      `.calibre-readout` и `.calibre-caption` скрыты (тесно, вертикальный
      текст на телефоне неуместен). `?v=` → `20260903-z8`. Проверено на
      375 / 390 / 720 / 768: механизм виден и анимируется, горизонтального
      скролла нет (вылеты орбит/лазеров срезает `.hero{overflow:hidden}`),
      консоль чистая, порядок в hero — текст → факты → механизм → секция
      Scope.
- [x] **З9. Мобильный: полусфера за списком фактов** *(2026-09-03, по фидбеку
      + скриншот-разметка заказчика)* — на телефоне механизм из блока З8
      «занимал целое место». Заказчик: показывать **половину сферы (~60%)**
      справа, ~40% уходит за правый край, по вертикали — от разделителя над
      «01» до конца «03», за текстом фактов. Перед правкой сделан бэкап
      (`_backup/2026-09-03-z8-before-mobile/`).
      **`@media(max-width:1000px)`** — центрированная версия З8 ужата до
      «контейнерной» (диск `min(52vw,280px)`, `min-height:min(58vw,300px)`);
      это фолбэк для 601–1000px.
      **`@media(max-width:720px)`** — механизм выведен из потока:
      `.hero-copy{position:relative;z-index:1}`, `.calibre-wrap{position:absolute;
      z-index:0;left:44vw;bottom:-10vw;width:94vw;height:94vw;opacity:.85;
      pointer-events:none}`, `.calibre{width:100%}`. Позиционируется в
      `.hero-layout` (у него уже `position:relative`), после ухода механизма из
      потока низ `.hero-layout` = низ фактов, поэтому `bottom:-10vw` сажает
      сферу по центру списка. `left:44vw` + `width:94vw` (единицы vw → масштаб
      по экрану) → на 375–430px ровно **~60% видно**, диск совпадает с блоком
      «01…03», лишнее срезает `.hero{overflow:hidden}`. `?v=` → `20260903-z9`.
      Проверено (computed + рендер) на 375 / 390 / 720 / 1280: видимая доля
      0.59–0.60, горизонтального скролла нет, десктоп не тронут
      (`.calibre-wrap` там `position:relative`, диск 430px), консоль чистая.
      Кандидаты на подстройку по фидбеку: положение «RDK» у края, прозрачность
      под текстом, точный `left`/`bottom`.
- [x] **З10. Скорость и плавность анимации (фидбек с iPhone)** *(2026-09-03)*
      — на телефоне (WebKit): точки по орбитам летели, одна из трёх шла в
      обратную сторону, кольца не вращались как на десктопе, картинка лагала.
      Правки (применяются на всех платформах — «как на компьютере» = общая
      скорость):
      1. **Обратная точка** — у `.orbit-c i` и у кольца `.orbit-b` убрано
         `reverse`; теперь всё движется в одну сторону.
      2. **Скорости снижены** — кольца `rev-a/-b/-c` `15/21/27с → 24/32/40с`;
         точки `run` `5/6.5/8с → 12/15/18с`; сфера `disc-spin` `8с → 12с`.
         Reduced-motion — тоже медленнее (150/200/250с, 48/58/68с).
      3. **`background-position` → `transform`** — вращение сферы
         (`.disc-sphere`) переведено на слой `.disc-sphere::before` с
         `translateX` (композитится на GPU, не перерисовывает слой каждый
         кадр). Убран слой бегущей полутени `disc-shade` целиком.
      4. **`backdrop-filter:blur(2px)` у `.disc-glass` убран** — на iOS это
         дорого и в связке с 3D ломает рендер; молочность компенсирована
         градиентом/тенью стекла.
      5. **JS-параллакс отключён на тач-устройствах** — в
         `initCalibreMotion()` ранний выход при `!finePointer`: на телефоне
         покадровый пересчёт всего `preserve-3d`-поддерева убран, движение
         несёт только CSS. `--motion-*` фиксируются в 0.
      `?v=` → `20260903-z10`. Проверено (computed + рендер, 390 / 1280):
      направления нормализованы, длительности применились, `disc-spin` на
      `::before` translateX анимируется, `backdrop-filter:none`, на тач
      `--motion-*` = 0 без rAF-цикла, keyframe `disc-shade` удалён без
      «сирот», горизонтального скролла нет, десктоп-раскладка не тронута,
      консоль чистая. **Плавность на реальном iOS проверяет заказчик.**
- [x] **З11. Кольца не вращались на iPhone — фикс поворота на 360°**
      *(2026-09-03)* — после З10 заказчик подтвердил: точки, скорость,
      направление, лаги, мерцание — ок, но **кольца всё ещё стоят на Safari**.
      Причина: WebKit анимацию поворота ровно на 360° сводит к матрице, а
      `rotate(0°)` и `rotate(360°)` дают одну и ту же матрицу → интерполяция
      «тождество → тождество», кольцо не двигается (Chrome обходит сам).
      `@keyframes rev-a/-b/-c` из одного шага `to{…360deg}` разбиты на пять
      (`0/25/50/75/100%` по 90°); наклон (`rotateX/rotateZ`) на каждом кадре
      **постоянный** — вращение остаётся ровным одноосным, без «кувыркания».
      `?v=` → `20260903-z11`. Проверено (Chromium): 5 шагов в каждом keyframe,
      `.orbit-*` transform прогрессирует, горизонтального скролла нет, консоль
      чистая. **Вращение колец на iOS проверяет заказчик.**
- [x] **З12. Десктоп: калибровка под ощущение с телефона** *(2026-09-03)* —
      заказчик: на iPhone механизм даёт «вау-эффект», всё чётко; на компьютере
      просит тот же «режим» по скорости и орбиты чуть заметнее. Диск на
      десктопе крупнее (430px против ~360px видимой половинки на телефоне) →
      та же угловая скорость выглядит быстрее. Добавлен блок
      `@media(min-width:1001px)` (десктоп; мобильные `≤720`/`≤1000` не
      затронуты, `≤1000` и `≥1001` не пересекаются):
      • скорость: кольца `24/32/40с → 32/42/52с`, точки `12/15/18с →
        16/20/25с`, вращение сферы `12с → 16с`;
      • видимость орбит: обводка `.orbit` `.3 → .4`, `.orbit-a` (бирюза)
        `.3 → .38`, `.orbit-c` `.24 → .32`, под-кольца `.15/.1 → .19/.13`.
      Reduced-motion (`motion.css`, `!important`) по-прежнему перебивает эти
      длительности. `?v=` → `20260903-z12`. Проверено (authored + computed,
      390 / 1280): блок применяется только `≥1001px`, мобильные значения
      (rev-a 24с, run 12с, обводка `.3`) не изменились, горизонтального
      скролла нет, консоль чистая.
- [x] **З13. Откат замедления десктопа + убран слой стекла** *(2026-09-03)* —
      заказчик: «сделай вращение на 24с на компьютере» (= как на телефоне,
      замедление З12 оказалось лишним) и «убери этот эффект стекла в общем».
      1. Из блока `@media(min-width:1001px)` убраны все `animation-duration` —
         десктоп вернулся к базовым скоростям (кольца `24/32/40с`, точки
         `12/15/18с`, сфера `12с`), т.е. равен телефону. Осталась только
         прибавка видимости орбит из З12 (обводки `.4 / .38 / .32`,
         под-кольца `.19 / .13`).
      2. **Слой `.disc-glass` удалён полностью** (на всех платформах): убран
         `<div class="disc-glass">` из `index.html`, правило `.disc-glass{…}`
         из `styles.css`, из `motion.css` — `.disc-glass{overflow:hidden}`,
         `.disc-glass::after`, `@keyframes glass-glint` и строка блик-эффекта
         в reduced-motion. Диск теперь матовый: объём держат радиальный
         градиент и inset-тени `.calibre-disc`, глянец ядра — `.disc-core::after`.
      `?v=` → `20260903-z13`. Проверено (390 / 1280): `.disc-glass` нет ни в
      DOM, ни в CSS, `@keyframes glass-glint` нет, слои диска — brush / scale /
      sphere / core, `rev-a` = 24с на всех ширинах, блок `≥1001px` = только
      цвета обводки, горизонтального скролла нет, консоль чистая, CSS
      парсится (186 правил).
- [x] **З14. Глобус на диске → сетка меридианов/широт** *(2026-09-03)* —
      заказчик: на основном глобусе только вертикальные линии, нужна сетка
      как на карте — меридианы «по диагонали», разметка линий. Слой
      `.disc-sphere` переделан из бегущих вертикальных полос
      (`::before` + `translateX`) в **проволочный каркас**:
      `index.html` — внутри `.disc-sphere` добавлен `.ds-globe` с 6 `<i>`.
      `styles.css` — `.ds-globe` (`preserve-3d`, `animation:disc-spin 26с`) —
      вращающаяся клетка; `.disc-sphere i` — кольца-меридианы
      `border-radius:50%` с `rotateY` 0/30/60/90/120/150° (проекцией дают
      эллипсы-дуги = «диагональные» меридианы); `.disc-sphere::after` —
      статичные широты (`repeating-linear-gradient(0deg)`), `rotate(7deg)`.
      `@keyframes disc-spin` из `translateX` → `rotateZ(8deg) rotateX(11deg)
      rotateY(0→360)` пятью шагами по 90° (наклон = «глобус под углом»,
      шаги 90° = Safari-безопасно). `opacity` слоя `.5 → .55`. `motion.css`
      — reduced-motion переведён с `.disc-sphere::before` на `.ds-globe`
      (64с); комментарий обновлён. `?v=` → `20260903-z14`. Проверено
      (390 / 1280): `.ds-globe` + 6 меридианов, `disc-spin` 5 шагов rotateY,
      `::after`-широты, старые полосы `::before` убраны, горизонтального
      скролла нет, десктоп-раскладка не тронута, консоль чистая.
- [x] **З15. Глобус: центрирование, плотность линий + десктоп 44с**
      *(2026-09-03)* — по фидбеку с телефона: (1) после З14 сфера визуально
      «сместилась» — виноват сильный наклон каркаса `rotateZ(8°) rotateX(11°)`
      + смещённая маска. Наклон снижен до `rotateX(5°)` (из `@keyframes
      disc-spin` убраны `rotateZ` и большой `rotateX`), маска
      `.disc-sphere` перецентрирована `ellipse 66%/92% at 46%/41%` →
      `80%/88% at 50%/47%`, у `::after`-широт убран `rotate(7°)`. Раскладка
      (`.calibre-wrap` t:615/b:981 на 390px) не менялась — сместилась только
      картинка. (2) «Сетка бледновата» → `.disc-sphere` opacity `.55→.62`,
      меридианы `.28→.38`, широты `.15→.22`. (3) «Вращение на 44с на
      компьютере» → в `@media(min-width:1001px)`: орбиты
      `rev-a/-b/-c 24/32/40с → 44/58/72с`, глобус `.ds-globe 26с → 44с`
      (мобильные значения не тронуты). `?v=` → `20260903-z15`. Проверено
      (390 / 1280): `disc-spin` = `rotateX(5°) rotateY(…)` 5 шагов, маска
      центрирована, opacity/цвета применились, десктопные длительности
      44/58/72/44с только `≥1001px`, `.calibre-wrap` на месте, горизонтального
      скролла нет, консоль чистая.
- [x] **З16. Десктоп: орбиты быстрее** *(2026-09-03)* — заказчик: «скорость
      орбит не увеличивается, сделай быстрее на десктопе» (в З15 «44с» было
      про глобус, орбиты замедлились ошибочно). В `@media(min-width:1001px)`:
      кольца `rev-a/-b/-c 44/58/72с → 16/22/28с` (быстрее и мобильных
      24/32/40), точки `run` тоже ускорены `→ 8/10/12с`. Глобус `.ds-globe`
      остался 44с. Мобильные значения не тронуты. `?v=` → `20260903-z16`.
      Проверено (1280): десктопные `animation-duration` = 16/22/28 (кольца),
      8/10/12 (точки), 44с (глобус) — только `≥1001px`; CSS парсится
      (194 правила), горизонтального скролла нет, консоль чистая.
- [x] **З17. Фиксация механизма** *(2026-09-03)* — заказчик проверил З16 на
      телефоне и компьютере: «теперь хорошо». Правок кода нет; `?v=` →
      `20260903-z17` во всех трёх ссылках (`styles.css`, `motion.css`,
      `script.js`) — гарантированный сброс кэша. Блок З (переработка hero-
      механизма: возврат формата → орбиты → вращение → мобильная полусфера →
      iOS-фиксы → сетка-глобус → калибровка скоростей) закрыт и принят.

## [2026-09-02] — визуальная ревизия

Задача: макет читался слишком мелким и малоконтрастным, иерархия смазана,
секции почти не отличались по тону. Работа велась блоками А–Д
(А1 → А2 → А3 → Б1 → Б2 → Б3 → В1 → В2 → Г1 → Г2 → Д1 → Д2), каждый подблок —
цикл «правка → проверка в браузере → запись».

### А — Фундамент (токены и текст)

- **А1. Токены** — в `:root` добавлены шкала кеглей
  (`--fs-micro` 12 / `--fs-body-s` 14 / `--fs-body` 16–17 / `--fs-lead` 19–21 /
  `--fs-h3` 20–28 / `--fs-h2` 36–58 / `--fs-h1` 44–78), шкала отступов
  (`--space-1`…`--space-10`: 4/8/12/16/24/32/48/64/96/128), радиусы
  (`--radius` 8 / `--radius-lg` 16 / `--radius-pill` 999).
- **А2. Цветовая система** — поверхности разведены по тону
  (`--surface` #ece9e1, `--surface-soft` #e2e6e0, `--surface-deep` #d3dad3,
  добавлен `--surface-ink` #22251f); контраст поднят (`--muted` #67685f,
  `--faint` — позже в Д2 доведён до #71726b); описательные абзацы переведены
  с `--muted` на `--text`.
- **А3. Типографика** — заголовки h1–h3 вес 600 (blockquote 500 — 600 на крупной
  цитате тяжело), трекинг −.04em → −.02em, line-height h1 1→1.05 / h2 1.06→1.1;
  все кегли 8/9/10/11/13/15 px заменены на токены; uppercase оставлен только у
  `.eyebrow` и одиночных технических меток (`.status-line`, `.contact-link small`);
  жёсткие `<br>` в h1/h2 убраны.

### Б — Ритм и раскладка

- **Б1. Вертикальный ритм** — вертикальные `margin`/`padding` между блоками на
  шкалу `--space-*`; `--section` clamp(96,12vw,164) → clamp(80,9vw,120),
  мобильный 88 → 72; надзаголовок ↔ заголовок 25 → 32, заголовок ↔ вводный
  абзац 30 → 48; hero-ритм 24/24/32/64; футер 105 → 96.
- **Б2. Hero** — колонки 1.12/.88 → 1.18/.82 (вторая `minmax(300px,.82fr)`);
  `.calibre` min(41vw,500) → min(35vw,430), `.calibre-wrap` уменьшен и сдвинут
  вверх на 24px через `margin-top` (`transform` перебивается системой `.reveal`);
  `.hero-facts` перестроены — номер-акцент сверху, описание `--fs-body-s` снизу;
  вертикальные подписи механизма 9 → 11px.
- **Б3. Карточки и сетки** — строки прайса: `min-height` 122 → 148, паддинг
  22/8 → 32/8, колоночный `gap` 28 → clamp(24,3vw,44), метка ↔ название 10 → 12;
  пропорции сеток сведены: `.method-layout` .74/1.26 → 1fr 2fr,
  `.price-disclaimer` без хака `margin-left:calc(…)` → full-width 1fr 2fr,
  `.contact-layout` 1.18/.82 → 1.15/.85 (= `.heritage-layout`);
  `.offer-index` намеренно оставлен узким рельсом вкладок.

### В — Цвет в деле и акценты

- **В1. Тёмная якорная секция** — блок `.principle` на `--surface-ink` (#22251f),
  текст #e9e8e0, метка «PRINCIPLE / 001» осветлённым акцентом #4fb3b6,
  паддинг увеличен до clamp(88,9vw,128). Единственная тёмная опора на длинной
  светлой странице.
- **В2. Работа акцента** — рекомендованный пакет: акцентная полоса слева
  (`box-shadow:inset 3px 0 0`) + плашка «Рекомендуем» (`::after`, пилюля);
  активная вкладка услуг — акцентный индикатор (слева / снизу / рамка по
  брейкпоинтам); круг-ссылка в строке прайса 32 → 40px.

### Г — Полировка

- **Г1. Иконки** — единый инлайн-SVG (`<symbol id="i-arrow">` в скрытом спрайте,
  14 использований через `<use>`); класс `.ico` (`width:1em`,
  `stroke:currentColor`), варианты `.ico--down` / `.ico--up` поворотом.
  Текстовые `↗ ↓ ↑` убраны.
- **Г2. Мелочи** — радиусы 6/16/999 px сведены к `--radius` / `--radius-lg` /
  `--radius-pill` (круги `50%` не тронуты); `.hero-foot` и `figcaption`
  8–9 → 11px.

### Д — QA

- **Д1. Адаптив** (прогон 375 / 700 / 768 / 1280) — мобильные h1
  clamp(40,12.2vw,58) → clamp(34,9.5vw,50) и h2 clamp(37,11vw,53) →
  clamp(32,8.6vw,46) (ушёл орфан «Код,»); на `<1000` скрыты вертикальные
  подписи механизма (налезали на лид при ghost-диске); мобильное меню —
  сплошной фон вместо полупрозрачного; мобильный `.offer-panel-head` поджат.
- **Д2. Финал** — аудит контраста по всем фонам (формула WCAG). Правки:
  `--faint` #82837b → #71726b; фон `.contact` #dde2de → #e7e5dd (был слишком
  тёмным для вторичного текста); информативные подписи (`.panel-note`,
  `.heritage-note`, `.method-list small`, `.contact-meta`, футер) переведены
  на `--muted`. Итог: основной текст 8.6–12.6:1, метки и вторичный текст
  ≥ 4.5:1, декоративные mono-индексы ~4.4:1. `?v=` в подключении CSS/JS
  обновлён (`?v=20260902-d2d`).

### Пост-ревизия — правки по фидбеку

- **Масштаб хедера** *(2026-09-03)* — оставался в дореформенном размере
  (высота 76, кружок 31, «RDK» 13, «IT ENGINEERING» 8px, ссылки 12px) —
  на фоне h1 92px / табов 16px / IBM Plex-меток смотрелся мелко. Подтянут
  «чуть-чуть», раскладка та же (лого слева / навигация справа):
  высота 76 → 84 (мобильная 68 → 74), кружок 31 → 36, «RDK» 13 → 15,
  «IT ENGINEERING» 8 → 11 (трекинг поджат), ссылки 12 → 14 (`--fs-body-s`),
  кнопка padding 10/14 → 11/16 + `font-weight:600`, gap навигации чуть шире.
  Смещения под фикс-хедер: `scroll-margin-top` 86 → 96, `.offer-nav` sticky
  110 → 118, `.method-intro` 120 → 128. В диапазоне 721–1000px навигация
  осталась компактной (12px, тесный gap, меньше паддинг кнопки), чтобы
  3 ссылки + кнопка не переносились до появления гамбургера (`≤720`).

- **Заголовок секции услуг** *(2026-09-03)* — «Сначала — понятный масштаб.
  Потом — точная смета.» на мобильном рвался на 5 огрызков с висячими тире.
  → текст сжат до «Сначала разбор&nbsp;— потом точная смета.» (одно тире,
  приклеено `&nbsp;` к «разбор», сохранён ритм сначала/потом); `.section-heading`
  переведён с 2-колоночной раскладки (заголовок | вводный абзац) на
  вертикальную (абзац под заголовком, `max-width` 920/560) — заголовок получил
  ширину и теперь 2 строки и на мобильном, и на десктопе.

- **Вкладки услуг** («Цифровые продукты / Боты и автоматизация / Инженерия и
  поддержка») были слишком мелкими (`--fs-micro` 12px) для основного
  переключателя категорий → `--fs-body` (16px), вес 500; рельс расширен
  `minmax(210px,.3fr)` → `minmax(256px,.34fr)`, номер-маркер выровнен по
  базовой линии.

### QA-прогон всех разрешений (2026-09-02)

Проверено на 375 / 700 / 720 / 768 / 1000 / 1001 / 1024 / 1100 / 1280 / 1440 / 1920.

**Найдено и исправлено:**
- **Горизонтальный скролл на ~1001–1080px** — сетка строки прайса
  (`minmax(160)+minmax(220)+minmax(140)+40` + gaps) была шире колонки панелей,
  пока раскладка услуг ещё 2-колоночная (залипающий рельс), а вьюпорт узкий.
  → мин-ширины ужаты до `120/150/96/40`, gap `clamp(24,3vw,44)` → `clamp(16,2.4vw,44)`.
- **`.contact-layout` оставался 2-колоночным на 721–1000px** (заголовок 356 /
  сайд 263 — тесно). Правило схлопывания было в блоке `≤720`, а не `≤1000`,
  как у остальных 2-колоночных секций. → перенесено в `≤1000` + `align-items:start`.

**Полировка мобильных отступов** (были подобраны под десктоп, на телефоне
слишком большие пустоты):
- `--section` на `≤720` 72 → 56px;
- `.contact-layout` gap 56 → 32px, `.contact-link` margin-top → `--space-6`;
- футер `margin-top` 96 → 48px на `≤720`.
Высота страницы на мобильном уменьшилась на ~300px.

**Проверено ОК:** нет горизонтального скролла ни на одной ширине; все сетки
схлопываются на своих брейкпоинтах; переключение вкладок услуг; мобильное меню
(открытие, сплошной фон, иконка «X», центрированные пункты, CTA); deep-teal
полоса «Принцип» на всех ширинах; рамка механизма (контейнирована на десктопе,
скрыта `<1000`); IBM Plex Mono (latin+cyrillic); кольцо `.contact-orbit` уходит
за экран, но обрезается `.contact{overflow:hidden}` — так и задумано; консоль
без ошибок.

### Что НЕ трогали

- Структуру HTML и порядок секций.
- Логику JS (анимации, меню, вкладки).
- Сам 3D-механизм (только размер и позицию).
- Палитру-концепцию (бежево-шалфейный + бирюза) — только развели тон.

### Известные ограничения (инструментальные, не дефекты сайта)

Встроенный браузер периодически перестаёт композитить эту страницу при
ширине ≥ ~720px: скриншоты ниже первого экрана выходят белыми, клики по
`computer` таймаутятся, CSS-переходы «замирают» в стартовом состоянии
(из-за этого `getComputedStyle` показывал меню как `opacity:0`, хотя правило
`.nav.is-open` корректно — проверено принудительным `transition:none`).
На реальных браузерах и на мобильной ширине всё работает. Проверка нижних
секций и интерактива на десктопе велась через computed styles + DOM + рендер
на мобильной ширине.
