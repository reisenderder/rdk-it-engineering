# План 04: Аналитика и учет посещений сайта

## Статус: ✅ Завершено (Completed)
**Ветка:** `feat/compliance-cookies-spam-warning`  
**Дата завершения:** 2026-09-22  
**Цель:** Организовать анализ посещаемости сайта в полном соответствии с законодательством РФ (Яндекс.Метрика + автономный микро-трекер).

---

## Задачи

- [x] **4.1. Интеграция Яндекс.Метрики (`index.html`, `script.js`)**
  - Подключен официальный асинхронный код счётчика Яндекс.Метрики (серверы в РФ, поддержка Вебвизора, карты скролла и кликов).
  - Настроена переменная `window.RDK_YM_ID` с понятной инструкцией в коде для подстановки ID счётчика при регистрации на `metrika.yandex.ru`.
  - В `script.js` при успешной отправке заявки реализован вызов цели:
    `ym(window.RDK_YM_ID, 'reachGoal', 'lead_submit', { service: payload.service });`

- [x] **4.2. Автономный микро-трекер визитов (`tracker.php`)**
  - Создан легковесный PHP-скрипт `tracker.php` для фиксации визитов на сервере Timeweb:
    - Дата и время (МСК).
    - URL страницы и реферер (источник визита).
    - Тип устройства (Mobile / Desktop / Tablet) по User-Agent.
    - **Анонимизированный IP-адрес** (маскирование последней части IP: `192.168.1.***` согласно 152-ФЗ и GDPR).
    - Безопасное хранение в `_visits.log.php` с директивой `<?php exit; ?>` на первой строке (защита от прямого скачивания).
    - Защищённый веб-дашборд просмотра статистики (`tracker.php?view=1&key=SECRET`) с авторизацией по ключу из `config.php`.
  - В `script.js` добавлена фоновая неблокирующая отправка через `navigator.sendBeacon` (с fallback на `fetch`).

- [x] **4.3. Тестирование и верификация**
  - Проверена корректность асинхронного вызова, отсутствие ошибок в консоли, корректность структуры JSON в логгере.
  - Обновлены версии кэша `styles.css?v=20260922-v21` и `script.js?v=20260922-v21`.

- [x] **4.4. Фиксация в `CHANGELOG.md` и Git коммит**
